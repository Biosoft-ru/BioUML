/* Generated By:JJTree: Do not edit this line. BNGReaction.java Version 4.3 */
/* JavaCCOptions:MULTI=true,NODE_USES_PARSER=false,VISITOR=false,TRACK_TOKENS=true,NODE_PREFIX=BNG,NODE_EXTENDS=,NODE_FACTORY=,SUPPORT_CLASS_VISIBILITY_PUBLIC=true */
package biouml.plugins.bionetgen.bnglparser;

public class BNGReaction extends SimpleNode
{
    protected boolean isReversible = false;

    public boolean isReversible()
    {
        return isReversible;
    }

    public void setReversible(boolean isReversible)
    {
        this.isReversible = isReversible;
        for( int i = 0; i < jjtGetNumChildren(); i++ )
        {
            biouml.plugins.bionetgen.bnglparser.Node child = jjtGetChild(i);
            if( child instanceof BNGSimpleElement && child.getName().contains("->") )
            {
                BNGSimpleElement newElement = new BNGSimpleElement(BionetgenParser.JJTSIMPLEELEMENT);
                if( isReversible )
                    newElement.setName("<->");
                else
                    newElement.setName("->");
                newElement.transferSpecialTokens((BNGSimpleElement)child);
                jjtAddChild(newElement, i);
                newElement.jjtSetParent(this);
                break;
            }
        }
    }

    public BNGList getReactionComponents(String role)
    {
        for( int i = 0; i < jjtGetNumChildren(); i++ )
        {
            Node child = children[i];
            if( child instanceof BNGList && ( (BNGList)child ).getType() == BNGList.REACTIONCOMPONENT && role.equals(child.getName()) )
                return (BNGList)child;
        }
        return null;
    }

    public BNGRateLaw getRateLaw()
    {
        for( int i = 0; i < jjtGetNumChildren(); i++ )
        {
            if( children[i] instanceof BNGRateLaw )
                return (BNGRateLaw)children[i];
        }
        return null;
    }

    @Override
    public String toBNGString()
    {
        return "";
    }

    public BNGReaction(int id)
    {
        super(id);
    }

    public BNGReaction(BionetgenParser p, int id)
    {
        super(p, id);
    }
}
/* JavaCC - OriginalChecksum=d2fc2b920058f9db5959e453ce8d0fa6 (do not edit this line) */
